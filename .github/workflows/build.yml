name: Build ZMK firmware
on:
  workflow_dispatch:
  push:
    paths-ignore:
      - "keymap-drawer/**"

jobs:
  build:
    uses: https://github.com/zmkfirmware/zmk/tree/main/.github/workflows/build-user-config.yml@main # point to github
