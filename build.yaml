---
include:
 # - board: eyelash_sofle_left
#    shield: nice_view
  - board: eyelash_sofle_right
#   shield: nice_view_custom
    shield: nice_view
  - board: eyelash_sofle_left
    shield: nice_view
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_STUDIO_LOCKING=n
    artifact-name: eyelash_sofle_studio_left
  # settings_reset is not buildable with darknao's ZMK fork (rgb-layer-25.08)
  # because activity.c unconditionally subscribes to zmk_split_peripheral_layer_changed
  # which is only defined in split keyboard builds.
  # - board: nice_nano_v2
  #   shield: settings_reset
